# Local .terraform directories
**/.terraform/*

# Terraform lockfile
.terraform.lock.hcl

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Exclude all .tfvars files, which are likely to contain sentitive data, such as
# password, private keys, and other secrets. These should not be part of version
# control as they are data points which are potentially sensitive and subject
# to change depending on the environment.
*.tfvars


# Öncelikle "*" ibaresinin anlamı önce veya sonra herhangi bir ifade demek.


# Burada belirttiğimiz dosyaları "git push" komutu girdiğimizde muaf kılıp gitmemeleri için yapıyoruz. Çünkü örneğin .tfvars bana özel değişken tanımlarının olduğu dosyadır ya da .terraform dosyası çok büyüktür, çünkü init diyince aws provider'ın versiyonu iniyor, github'a giderken sorun çıkabilir ya da gitmeyebilir.